spring:
  datasource:
    url: jdbc:h2:mem:groo_test;MODE=MYSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false
  flyway:
    enabled: false
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 15
      repositories:
        enabled: false

app:
  security:
    jwt:
      secret: test-secret-test-secret-test-secret-test-secret
